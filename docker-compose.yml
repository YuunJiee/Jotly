services:
  jotly:
    build: .
    image: jotly:latest
    container_name: jotly
    restart: always
    ports:
      - "127.0.0.1:8001:8000"
    volumes:
      - ./db.sqlite3:/app/db.sqlite3
    env_file:
      - .env
